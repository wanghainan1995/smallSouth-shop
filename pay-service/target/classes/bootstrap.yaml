spring:
  application:
    name: pay-service # 服务名称
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    port: 5672
    virtual-host: /
    username: guest
    password: guest
  cloud:
    nacos:
      server-addr: 172.18.5.92:8848 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: shared-jdbc.yaml
          - dataId: shared-log.yaml
          - dataId: shared-swagger.yaml
          - dataId: shared-feign.yaml
#          - dataId: shared-rabbitmq.yaml
        namespace: f3083ee1-3163-4d05-a53e-0463864b38d3